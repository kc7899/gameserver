// router.js by benvar handle = {};var info = require("./fruit/info");handle["/fruit/info"] = info.get_info_data;function route(pathname,querystring){//    console.log(pathname, querystring);    var patch = "."+pathname;//    if(mkdir_p(patch+".js"))//    {//        var request = require(patch);//        return request.info_request(querystring);//    }//    else//    {//        return "404";//    }    if (typeof handle[pathname] == "function") {        return handle[pathname](querystring);    } else {        console.log("No request handler found for " + pathname);        return "404";    }}exports.route = route;